#+TITLE: Project Modernization
#+STARTUP: overview

* Fix Build

** DONE Fix ~-Werror~ failures so Debug build passes
Fixed explicit void* casts, trailing semicolons, goto/init ordering,
and LuaSkin framework header diagnostics across all extensions.

** NEXT Enable ~GCC_TREAT_WARNINGS_AS_ERRORS~ project-wide
Currently commented out in ~Project-Base.xcconfig~ with a FIXME.
Extensions already have it via ~Extensions-Ideal.xcconfig~.
Uncomment it in ~Project-Base.xcconfig~ so the app target is also covered.

* Clean Up Stale References

** DONE Remove empty Pods group from ~project.pbxproj~
** DONE Update ~CONTRIBUTING.md~
** DONE Remove stale ~Pods~ header search path

* Build Config Cleanup

** DONE Fix typo in ~Extensions-Base.xcconfig~
** NEXT Resolve ~EXECUTABLE_PREFIX~ FIXME
~Extensions-Base.xcconfig~ sets ~EXECUTABLE_PREFIX = lib~ with a FIXME asking
whether the copy script can be adjusted instead. Investigate and either remove
the FIXME or fix the script.

** NEXT Enable ~ENABLE_USER_SCRIPT_SANDBOXING~
Xcode warns that unsandboxed script phases delay ~httpserver~ target's Copy Headers.
Set ~ENABLE_USER_SCRIPT_SANDBOXING=YES~ where possible.

* Tech Debt

** DONE Fix ~hs.axuielement~ warnings
** DONE Fix ~hs.doc~ shadow warnings
** DONE Audit deprecated API usage (pasteboard)
** NEXT Migrate deprecated ~typeOfFile:error:~ API in ~MJAppDelegate.m~
Uses deprecated ~NSWorkspace typeOfFile:error:~. Migrate to
~-[NSURL getResourceValue:forKey:error:]~ with ~NSURLContentTypeKey~.

** NEXT Fix pre-existing ~extensions/doc/stack.c~ markdown parser
The vendored sundown markdown library uses C patterns that conflict with
~-Weverything~. Consider replacing with a maintained alternative or
accepting the warning suppressions.
