#+TITLE: Build Simplification
#+STARTUP: overview

* Vision

Make ~swift build~ the primary tool for everything that doesn't need an
~.app~ bundle, and reduce ~xcodebuild~ / ~project.pbxproj~ to the bare
minimum required to ship a signed, sandboxed AppKit application. Every
piece of code should live in an SPM package or be generated by a script
from a flat manifest — no Xcode-UI-only state.

Done so far: 91 extension dylibs → single SPM static library; LuaSkin →
SPM; per-extension Xcode targets removed (96 → 3); hs CLI → SPM; Lua
file copying → manifest-driven; script sandboxing enabled; ~-undefined
dynamic_lookup~ removed; xcconfigs collapsed 6 → 3; all 15 compiler
warnings enabled; PCH removed; incremental builds fixed; Package.swift
switched to auto-discovery; manifests unified; pbxproj replaced with
XcodeGen ~project.yml~; Run Script phases moved to ~justfile~.

* TODO — Modernization (macOS 26 Tahoe + Xcode 17)

*** TODO Rewrite ~notify~ extension: ~NSUserNotification~ → ~UNUserNotificationCenter~
    ~extensions/notify/libnotify.m~ (1518 lines). Complete rewrite.

* Investigated — not worth doing

** SKIP Replace ~.xib~ files with programmatic UI
   Controllers are coupled to app-level singletons regardless of XIBs.
   Half-day effort for low benefit.

** SKIP Make generators Xcode build-time phases
   Not feasible — generator outputs are SPM package sources (compiled
   before Xcode target phases run) and xcfilelists (consumed before the
   build starts). Pre-commit hook + CI is the correct enforcement point.

** DEFER Make ~BuildDocs~ a workspace SPM dependency
   Only practical option (Option C: ~.executableTarget~ invoked by Run
   Script) adds ~swift-argument-parser~ to workspace resolution for
   marginal benefit. Current out-of-band build works fine.

* Done

** DONE Merge 19 core ~.m~ files into ~HSExtensions~ SPM target
** DONE Move Run Script phases into ~just~ recipes
** DONE Replace ~.xcodeproj~ with XcodeGen ~project.yml~
** DONE Delete dead files (version script, PCH, blueprint, xccheckout)
** DONE Delete vestigial xcfilelists and generator
** DONE Fix ~codecov.yml~ stale paths
** DONE Align deployment targets (.v14 → .v15) across SPM packages
** DONE Deduplicate deployment target documentation
** DONE Gitignore ~project.xcworkspace/~ (XcodeGen-generated)
** DONE Remove redundant remote SPM deps from ~project.yml~
** DONE Fix post-build codesign regression
** DONE Switch ~Package.swift~ to SPM auto-discovery (246 → 105 lines)
** DONE Unify ~extensions.list~ + ~lua-files.list~ into ~extensions.manifest~
** DONE Delete orphaned ~hs.xcconfig~
** DONE Delete unused ~Hammerspoon-Test.xcconfig~
** DONE Remove empty "Embed Frameworks" build phase
** DONE Remove dead ~Hammerspoon-Downstream.xcconfig~ includes
** DONE Fix "Copy hs CLI" phase for incremental builds
** DONE Fix "Compile docs.json" output declaration
** DONE Collapse xcconfigs from 6 → 3
** DONE Document ~Hammerspoon-Profile.xcconfig~ overrides
** DONE Add pre-commit hook for generator staleness
** DONE Enable all 15 compiler warnings
** DONE Set ~GCC_OPTIMIZATION_LEVEL~ per-configuration
** DONE Remove the precompiled header
** DONE Add missing ~/* Copy hs CLI */~ pbxproj comment
** DONE Drive "Copy Extension Lua Files" from a manifest
** DONE Move the ~hs~ CLI to ~swift build~
** DONE Consolidate 91 extension dylibs into one SPM static library
** DONE Move LuaSkin into an SPM package
** DONE Remove per-extension Xcode targets (96 → 3)
** DONE Fix ~-Werror~ failures so Debug build passes
** DONE Enable ~GCC_TREAT_WARNINGS_AS_ERRORS~ project-wide
** DONE Remove empty Pods group from ~project.pbxproj~
** DONE Remove stale ~Pods~ header search path
** DONE Enable ~ENABLE_USER_SCRIPT_SANDBOXING~
** DONE Drop ~-undefined dynamic_lookup~
** DONE Bump deployment target to macOS 26
** DONE Modernize C/C++ language standards (~gnu17~ / ~gnu++17~)
** DONE Bump ~swift-tools-version~ to 6.2 (all 4 packages)
** DONE Remove weak-linking of SkyLight, DisplayServices, CoreDisplay
** DONE Remove all ~@available~ guards below macOS 26
** DONE Replace ~LSSharedFileList~ with ~SMAppService~
** DONE Replace ~activateIgnoringOtherApps:~ with ~activate~
** DONE Replace ~NSDisableScreenUpdates~ / ~NSEnableScreenUpdates~
** DONE Replace ~[AVCaptureDevice devices]~ with discovery session
** DONE Replace ~[CWInterface interface]~ with ~CWWiFiClient~
** DONE Replace ~ProcessSerialNumber~ / Carbon Process Manager
** DONE Update CocoaLumberjack 3.8.5 → 3.9.0
