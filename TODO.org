#+TITLE: Project Modernization
#+STARTUP: overview

* Fix Build

** NEXT Fix ~-Werror~ failures so Debug build passes
The build currently fails due to ~-Weverything~ + ~GCC_TREAT_WARNINGS_AS_ERRORS~ in
~Extensions-Ideal.xcconfig~. Two extensions are affected:
*** ~extensions/timer/libtimer.m~
- ~-Wimplicit-void-ptr-cast~ (5 sites) — add explicit casts
- ~-Wextra-semi-stmt~ (3 sites) — remove trailing semicolons from macros
*** ~extensions/streamdeck/~ (~HSStreamDeckDevice.m~, ~HSStreamDeckManager.m~)
- ~-Wimplicit-void-ptr-cast~ (7 sites) — add explicit casts
- ~-Wextra-semi-stmt~ (many sites) — remove trailing semicolons from macros
*** ~extensions/screen/libscreen.m~
- ~-Wimplicit-int-enum-cast~ — cast ~IOReturn~ to ~CGDisplayErr~
- ~-Wjump-misses-init~ — refactor goto to avoid jumping over variable init

** NEXT Enable ~GCC_TREAT_WARNINGS_AS_ERRORS~ project-wide
Currently commented out in ~Project-Base.xcconfig~ with a FIXME.
After fixing the above, uncomment it so new warnings can't slip in.

* Clean Up Stale References

** NEXT Remove empty Pods group from ~project.pbxproj~
Group ~0024EE4F07CEC6C274F32AB6~ is an empty leftover from CocoaPods removal.
Remove the ~PBXGroup~ entry and its reference in the parent group's ~children~ array.

** NEXT Update ~CONTRIBUTING.md~
Still references CocoaPods, ~Podfile~, and ~pod install~. Rewrite the build
instructions to reflect the current SPM-only setup.

** NEXT Remove stale ~Pods~ header search path
~Project-Base.xcconfig~ had ~HEADER_SEARCH_PATHS = $(SRCROOT)/Pods/**~ which was
cleared but should be fully removed (currently set to empty string).

* Build Config Cleanup

** NEXT Fix typo in ~Extensions-Base.xcconfig~
~WARNING_CLAGS~ should be ~WARNING_CFLAGS~ (line 20). Currently the
~-Wno-tautological-pointer-compare~ and related flags have no effect.

** NEXT Resolve ~EXECUTABLE_PREFIX~ FIXME
~Extensions-Base.xcconfig~ sets ~EXECUTABLE_PREFIX = lib~ with a FIXME asking
whether the copy script can be adjusted instead. Investigate and either remove
the FIXME or fix the script.

** NEXT Enable ~ENABLE_USER_SCRIPT_SANDBOXING~
Xcode warns that unsandboxed script phases delay ~httpserver~ target's Copy Headers.
Set ~ENABLE_USER_SCRIPT_SANDBOXING=YES~ where possible.

* Tech Debt

** NEXT Fix ~hs.axuielement~ warnings
~Extensions-Base.xcconfig~ has a FIXME to fix ~hs.axuielement~ so the
~-Wno-tautological-pointer-compare~ and ~-Wno-bad-function-cast~ suppression
flags can be removed.

** NEXT Fix ~hs.doc~ shadow warnings
~Project-Base.xcconfig~ has ~GCC_WARN_SHADOW~ commented out because it breaks
~hs.doc~'s autolink dependency. Fix the underlying issue and enable the warning.

** NEXT Audit deprecated API usage
~extensions/pasteboard/libpasteboard.m~ uses deprecated ~NSArchiver~ APIs
(~unarchiveObjectWithData:~, ~archivedDataWithRootObject:~). Migrate to
~NSSecureCoding~ equivalents.
